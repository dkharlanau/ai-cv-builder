{
  "name": "ai-cv-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free reference builder for a portable professional profile.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/validate-profile.mjs examples/profile.example.json && node --check lib/profile.mjs && node --check scripts/build-site.mjs && node --check scripts/validate-profile.mjs",
    "test": "npm run check && node --test tests/*.test.mjs"
  }
}
